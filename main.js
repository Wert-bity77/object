let fert = prompt('Ismingizni kiriting')
let id = prompt('Idingizni kiriting')
let password = prompt('Passwordni kiriting')

const obj = {
    id: id,
    names: fert,
    password: password,
  
}

console.log(obj);
